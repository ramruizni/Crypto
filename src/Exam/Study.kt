package Exam

// Group <G, *> IF
// closed
// associative (a*b)*c = a*(b*c)
// neutral e*x = x*e = x
// inverse x*y = y*x = e

// Abelian Group IF
// commutative a*b = b*a

// Ring <A, +, *> IF // must be 2 operations
// <A, +> abelian group
// * associative
// distributive by left  a*(b+c) = a*b + a*c
// distributive by right (a+b)*c = a*c + b*c

// Commutative Ring IF
// * commutative

// Ring with Unity IF
// * has neutral

// Non trivial divisors of 0 IF
// a!=0, b!= && a*b = 0

// Integer Domain IF
// Ring with unity
// && without Non trivial divisors of 0

// Body <A, +, *> IF
// Commutative Ring with Unity
// With multiplicative inverses != 0
// 0 is neutral element of +
// ex: <Z, +, *> not body. <R, +, *> is. // also Q and C

// if p prime -> Zp is a Body
// Every Body is an Integer Domain

//----------------------------------------------------------//



